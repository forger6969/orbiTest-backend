# ⚡ Быстрый старт - Документация Swagger

## 🎯 За 5 минут до первого запроса

### Шаг 1: Установка зависимостей ✅

```bash
npm install
```

✓ Все зависимости уже установлены, включая `swagger-ui-express`

### Шаг 2: Запуск сервера

```bash
npm start
```

Сервер запустится на `http://localhost:5000`

### Шаг 3: Откройте Swagger UI

Перейдите в браузере на:

```
http://localhost:5000/api-docs
```

### Шаг 4: Первый запрос!

1. Нажмите на **Authentication** → **POST /api/auth/register**
2. Нажмите **"Try it out"**
3. Заполните данные:
   ```json
   {
     "username": "testuser",
     "email": "test@example.com",
     "password": "Password123",
     "firstName": "Test",
     "lastName": "User"
   }
   ```
4. Нажмите **"Execute"**
5. Готово! 🎉 Вы создали первого пользователя

---

## 📋 Чек-лист первых операций

- [ ] Регистрация пользователя
- [ ] Вход в систему и получение токена
- [ ] Авторизация в Swagger UI
- [ ] Получение информации профиля
- [ ] Просмотр всех тестов
- [ ] Просмотр всех групп
- [ ] Просмотр всех экзаменов

---

## 🔑 Как авторизоваться в Swagger

### Для защищенных эндпоинтов:

1. **Нажмите кнопку "Authorize"** в верхнем правом углу интерфейса

2. **Выберите "BearerAuth"** и введите ваш JWT токен (только токен, без "Bearer ")

3. **Нажмите "Authorize"**

Теперь все защищенные запросы будут автоматически включать ваш токен!

---

## 📁 Что было создано

```
📦 Проект обновлен:
├── 📄 swagger.json           ← Полная OpenAPI спецификация
├── 📄 swagger.setup.js       ← Конфиг Swagger UI
├── 📄 API_DOCUMENTATION.md   ← Подробная документация (на русском)
├── 📄 SWAGGER_GUIDE.md       ← Гайд по использованию Swagger UI
└── 📄 QUICK_START.md         ← Этот файл
```

---

## 🌐 URL для быстрого доступа

| Назначение           | URL                                  |
| -------------------- | ------------------------------------ |
| 🏥 Проверка здоровья | `http://localhost:5000/health`       |
| 📚 Swagger UI        | `http://localhost:5000/api-docs`     |
| 📋 JSON спецификация | `http://localhost:5000/swagger.json` |

---

## 🚀 Типичный рабочий процесс

### Для студента:

```
1. Зарегистрироваться (POST /api/auth/register)
2. Войти (POST /api/auth/login) → получить токен
3. Авторизоваться в Swagger
4. Просмотреть тесты (GET /api/test/all)
5. Выбрать тест (GET /api/test/get/{id})
6. Пройти тест (POST /api/test/result)
7. Смотреть результаты (GET /api/test/myResults)
```

### Для администратора:

```
1. Вход (POST /api/auth/login) → получить токен
2. Авторизоваться в Swagger
3. Создать группу (POST /api/group/create)
4. Добавить студентов (POST /api/group/add)
5. Создать тест (POST /api/test/create)
6. Создать экзамен (POST /api/exam/create)
7. Просматривать результаты студентов
```

---

## 💡 Полезные советы

### 💾 Сохраняйте ID для повторного использования

Когда вы создаете ресурс (тест, группу, экзамен), скопируйте ID из ответа. Его потребуется для других операций.

### 🔍 Используйте примеры

Большинство эндпоинтов имеют готовые примеры запросов. Просто нажмите на пример - он заполнится в поле автоматически.

### 🔐 Оставляйте авторизацию включенной

Авторизуйтесь один раз через "Authorize" и работайте со всеми защищенными эндпоинтами без повторной аутентификации.

### 📱 Копируйте curl команды

Щелкните правой кнопкой на запросе → "Copy as curl" для использования в терминале.

---

## ❓ Часто задаваемые вопросы

### Q: Где найти token?

**A:** После выполнения `POST /api/auth/login` в поле `token` ответа.

### Q: Как использовать token?

**A:** Нажмите "Authorize" в Swagger и вставьте токен.

### Q: Ошибка 401 Unauthorized?

**A:** Токен либо истек, либо неверен. Выполните вход заново.

### Q: Ошибка 403 Forbidden?

**A:** Вы не администратор. Используйте аккаунт администратора для создания тестов.

### Q: Где моя база данных?

**A:** Используется MongoDB. Убедитесь что сервер MongoDB запущен или подключен через облако.

---

## 🛠️ Отладка

### Просмотр логов сервера

```bash
# Логи выводятся в консоль при npm start
# Ищите ошибки, связанные с базой данных или валидацией
```

### Тестирование через curl

```bash
# Пример простого запроса
curl -X GET http://localhost:5000/health

# Пример запроса с токеном
curl -X GET http://localhost:5000/api/user/me \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 📚 Дополнительные ресурсы

1. **API_DOCUMENTATION.md** - Полная документация всех эндпоинтов
2. **SWAGGER_GUIDE.md** - Подробный гайд по использованию Swagger UI
3. **swagger.json** - Машиночитаемая OpenAPI спецификация

---

## 🎓 Примеры реальных сценариев

### Сценарий 1: Создание полного курса (администратор)

1. Создать группу для студентов
2. Создать несколько тестов
3. Создать экзамен с требованиями
4. Пригласить студентов присоединиться к группе

### Сценарий 2: Обучение (студент)

1. Зарегистрироваться
2. Присоединиться к группе (через ID)
3. Выполнить все тесты
4. Улучшить свой уровень квалификации
5. Подготовиться к экзамену

### Сценарий 3: Аналитика (администратор)

1. Просмотреть всех студентов в группе
2. Проверить результаты каждого студента
3. Анализировать успеваемость
4. Отправить напоминания через Telegram

---

## ✅ Все готово!

**Ваш API полностью документирован и готов к использованию!**

- ✅ Swagger UI интегрирован
- ✅ Все эндпоинты задокументированы
- ✅ Примеры и описания добавлены
- ✅ Безопасность настроена (JWT + Bearer Auth)
- ✅ Структуры данных описаны

Начинайте тестировать через http://localhost:5000/api-docs 🚀

---

**Дата создания:** 2025-01-27  
**Версия API:** 1.0.0  
**Статус:** Production Ready
