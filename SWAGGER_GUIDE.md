# 🚀 Swagger UI - Интерактивная документация API

## Как использовать Swagger UI

### 1. Запуск приложения

```bash
npm install
npm start
```

Приложение запустится на `http://localhost:5000`

### 2. Открытие Swagger UI

Откройте в браузере:

```
http://localhost:5000/api-docs
```

Вы увидите интерактивный интерфейс со всеми API эндпоинтами проекта.

## 📖 Структура документации Swagger

### Основные секции:

1. **Health Check** - Проверка статуса сервера
2. **Authentication** - Регистрация и вход
3. **User** - Профиль пользователя
4. **Tests** - Создание и прохождение тестов
5. **Groups** - Управление группами студентов
6. **Exams** - Создание и управление экзаменами

## 🔐 Аутентификация в Swagger

### Как авторизоваться:

1. Выполните `POST /api/auth/register` для создания аккаунта
   - Заполните все обязательные поля
   - Нажмите **"Try it out"**
   - Нажмите **"Execute"**

2. Выполните `POST /api/auth/login` для входа
   - Используйте email и пароль
   - В ответе получите JWT токен

3. Авторизуйтесь в Swagger:
   - Нажмите кнопку **"Authorize"** в верхнем правом углу
   - Выберите **"BearerAuth"**
   - Введите токен (без "Bearer " - только сам токен)
   - Нажмите **"Authorize"**

Теперь все защищенные эндпоинты будут использовать ваш токен!

## 🧪 Примеры тестирования

### Пример 1: Создание теста (Администратор)

1. **Авторизуйтесь** как администратор
2. Перейдите к `POST /api/test/create`
3. Нажмите **"Try it out"**
4. Заполните тело запроса:

```json
{
  "testType": "react",
  "testTitle": "React Basics Quiz",
  "testDescribe": "Тест на знание основ React",
  "testGrade": "junior",
  "gradeExperience": 5,
  "testTime": 1200000,
  "questions": [
    {
      "question": "Что такое JSX?",
      "variants": {
        "a": "JavaScript XML",
        "b": "Java Syntax Extension",
        "c": "JSON Extended",
        "d": "jQuery XML"
      },
      "correctAnswer": "a",
      "questionsScore": 10
    }
  ]
}
```

5. Нажмите **"Execute"**
6. Получите ID теста в ответе

### Пример 2: Прохождение теста (Студент)

1. **Авторизуйтесь** как студент
2. Перейдите к `GET /api/test/all`
3. Нажмите **"Execute"** - получите список тестов
4. Скопируйте ID нужного теста
5. Перейдите к `POST /api/test/result`
6. Заполните тело запроса:

```json
{
  "testId": "скопированный_id_теста",
  "answers": [
    {
      "questionId": "1",
      "answer": "a"
    }
  ]
}
```

7. Нажмите **"Execute"**
8. Получите результаты теста

### Пример 3: Управление группами (Администратор)

1. **Авторизуйтесь** как администратор
2. Перейдите к `POST /api/group/create`
3. Нажмите **"Try it out"**
4. Заполните данные группы:

```json
{
  "groupName": "Frontend Group",
  "groupDescribe": "Группа для обучения frontend разработке",
  "groupDay": "even",
  "groupTime": "14:00-16:00"
}
```

5. Нажмите **"Execute"**
6. Скопируйте ID созданной группы
7. Перейдите к `POST /api/group/add`
8. Добавьте студентов в группу:

```json
{
  "groupId": "скопированный_id_группы",
  "userId": "id_студента"
}
```

## 📝 Описание схем данных

Все типы данных полностью описаны в документации. При заполнении запроса вы сможете увидеть:

- Обязательные поля (marked with `*`)
- Типы данных для каждого поля
- Примеры значений
- Ограничения (min/max, enum и т.д.)

### Основные типы данных:

**User** - Пользователь системы

- Поля: username, email, firstName, lastName, grade, role
- Роли: user, admin
- Уровни: junior, strongJunior, middle, strongMiddle, senior

**Test** - Тест с вопросами

- Поля: testType, testTitle, questions, testGrade
- Типы: react, node, javaScript, python, html, css, vue

**TestResult** - Результат прохождения теста

- Поля: user, test, score, successRate, answers
- Хранит все ответы пользователя

**Group** - Группа студентов

- Поля: groupName, students, groupDay, groupTime
- День: even (четные), odd (нечетные)

**Exam** - Экзамен

- Поля: examTitle, examStart, examEnd, requirements, status
- Статусы: underway, completed

## 🔍 Просмотр ответов

### Для каждого запроса видны:

1. **Response body** - Полный ответ сервера
2. **Response headers** - Заголовки ответа
3. **Response code** - Код ответа (200, 201, 400, 401, 404, 500)

Нажмите на кнопку с кодом ответа для просмотра описания.

## 💡 Полезные советы

### 1. Используйте "Examples"

При заполнении запроса нажмите на пример слева от поля - это покажет готовый пример данных.

### 2. Скопируйте ответ

Нажмите на иконку копирования рядом с ответом, чтобы скопировать JSON.

### 3. Посмотрите шему запроса

Нажмите на "Schema" для просмотра полной структуры данных.

### 4. Используйте поиск

Воспользуйтесь функцией поиска (Ctrl+F) для быстрого поиска нужного эндпоинта.

## 🐛 Распространенные ошибки

### Ошибка 400 - Bad Request

**Причина:** Неверные данные в запросе
**Решение:**

- Проверьте что заполнены все обязательные поля
- Убедитесь в правильности типов данных
- Посмотрите на примеры в документации

### Ошибка 401 - Unauthorized

**Причина:** Отсутствует или неверный токен
**Решение:**

- Авторизуйтесь через `/api/auth/login`
- Нажмите "Authorize" и введите полученный токен

### Ошибка 403 - Forbidden

**Причина:** Не хватает прав (нужны права администратора)
**Решение:**

- Используйте аккаунт администратора

### Ошибка 404 - Not Found

**Причина:** Ресурс не найден
**Решение:**

- Проверьте правильность ID (в path параметрах)
- Убедитесь что ресурс существует

### Ошибка 500 - Internal Server Error

**Причина:** Ошибка на сервере
**Решение:**

- Проверьте логи сервера в терминале
- Убедитесь что база данных доступна

## 📚 Дополнительная документация

Полная документация доступна в файле `API_DOCUMENTATION.md`

Там вы найдете:

- Полное описание всех эндпоинтов
- Примеры использования (curl команды)
- Описание структуры данных
- Полные коды ошибок

## 🎯 Быстрая навигация

- Регистрация: `POST /api/auth/register`
- Вход: `POST /api/auth/login`
- Мой профиль: `GET /api/user/me`
- Все тесты: `GET /api/test/all`
- Мои результаты: `GET /api/test/myResults`
- Все группы: `GET /api/group/all`
- Все экзамены: `GET /api/exam/all`

---

**Версия API:** 1.0.0  
**Последнее обновление:** 2025-01-27
