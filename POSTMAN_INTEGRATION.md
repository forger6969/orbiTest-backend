# 📮 Интеграция с Postman

Полная инструкция по импорту API спецификации в Postman для удобного тестирования.

## 🚀 Быстрый импорт

### Способ 1: Импорт по URL (Рекомендуется)

1. **Откройте Postman** и нажмите кнопку **"Import"** в верхнем левом углу

2. **Выберите вкладку "Link"**

3. **Вставьте URL:**

   ```
   http://localhost:5000/swagger.json
   ```

4. **Нажмите "Continue"** и затем **"Import"**

5. **Готово!** 🎉 Все эндпоинты появятся в вашей коллекции

### Способ 2: Импорт JSON файла

1. **Скачайте swagger.json:**

   ```bash
   curl http://localhost:5000/swagger.json > swagger.json
   ```

2. **В Postman:** Import → Upload Files → выберите swagger.json

3. **Нажмите "Import"**

---

## 🔐 Настройка аутентификации в Postman

### Шаг 1: Получить токен

1. В импортированной коллекции найдите **"POST /api/auth/login"**

2. В разделе **Body** установите ваши учетные данные:

   ```json
   {
     "email": "your_email@example.com",
     "password": "your_password"
   }
   ```

3. Нажмите **"Send"**

4. В ответе скопируйте значение `token`

### Шаг 2: Сохранить токен в переменную

1. **Создайте переменную окружения:**
   - Нажмите на иконку окружений (обычно справа в меню)
   - Создайте новое окружение: "OrbiTest Dev"

2. **Добавьте переменную:**
   - **Variable:** `token`
   - **Value:** `<paste_your_token_here>`

3. **Сохраните**

### Шаг 3: Используйте токен в запросах

Все защищенные эндпоинты (с меткой 🔒) используют переменную `{{token}}`

Просто убедитесь, что в Postman выбрано правильное окружение!

---

## 🎯 Настройка базового URL

### Метод 1: Переменная окружения (Рекомендуется)

1. Откройте **Environments**

2. Создайте переменную:
   - **Variable:** `baseUrl`
   - **Development Value:** `http://localhost:5000`
   - **Production Value:** `https://api.orbitest.com`

3. Используйте `{{baseUrl}}` в начале каждого запроса

### Метод 2: Использование переменной коллекции

1. Откройте **Edit Collection**

2. В раделе **Variables** добавьте:
   - **Variable:** `baseUrl`
   - **Value:** `http://localhost:5000`

3. Все эндпоинты будут использовать эту переменную

---

## 📝 Структура импортированной коллекции

```
OrbiTest Backend API
├── Health Check
│   ├── GET /
│   └── GET /health
├── Authentication
│   ├── POST /api/auth/register
│   └── POST /api/auth/login
├── User
│   └── GET /api/user/me
├── Tests
│   ├── POST /api/test/create
│   ├── GET /api/test/all
│   ├── GET /api/test/get/{id}
│   ├── GET /api/test/types
│   ├── POST /api/test/result
│   └── GET /api/test/myResults
├── Groups
│   ├── POST /api/group/create
│   ├── POST /api/group/add
│   └── GET /api/group/all
└── Exams
    ├── POST /api/exam/create
    └── GET /api/exam/all
```

---

## 🔄 Полный workflow в Postman

### Сценарий 1: Тестирование как студент

```
1. Health Check → GET /health
   ↓
2. Authentication → POST /api/auth/register
   ├─ Заполните: username, email, password, firstName, lastName
   ↓
3. Authentication → POST /api/auth/login
   ├─ Скопируйте token в окружение
   ↓
4. User → GET /api/user/me
   ├─ Используйте {{token}}
   ↓
5. Tests → GET /api/test/all
   ├─ Найдите нужный тест
   ↓
6. Tests → GET /api/test/get/{id}
   ├─ Подставьте ID теста
   ↓
7. Tests → POST /api/test/result
   ├─ Отправьте ответы на вопросы
   ↓
8. Tests → GET /api/test/myResults
   ├─ Просмотрите результаты
```

### Сценарий 2: Управление как администратор

```
1. Authentication → POST /api/auth/login
   ├─ Используйте admin аккаунт
   ├─ Скопируйте token
   ↓
2. Groups → POST /api/group/create
   ├─ Создайте новую группу
   ├─ Скопируйте ID группы
   ↓
3. Groups → POST /api/group/add
   ├─ Добавьте студентов в группу
   ↓
4. Tests → POST /api/test/create
   ├─ Создайте новый тест
   ├─ Скопируйте ID теста
   ↓
5. Exams → POST /api/exam/create
   ├─ Создайте экзамен
   ├─ Укажите группу и требования
```

---

## 💾 Советы по работе в Postman

### 1. Используйте Pre-request Scripts

Для автоматического получения токена:

```javascript
// В Pre-request Script вкладке

// Если токена еще нет, запросить новый
if (!pm.environment.get("token")) {
  const loginRequest = {
    url: pm.environment.get("baseUrl") + "/api/auth/login",
    method: "POST",
    header: {
      "Content-Type": "application/json",
    },
    body: {
      mode: "raw",
      raw: JSON.stringify({
        email: "admin@example.com",
        password: "AdminPassword123",
      }),
    },
  };

  pm.sendRequest(loginRequest, (err, response) => {
    if (!err) {
      const token = response.json().token;
      pm.environment.set("token", token);
    }
  });
}
```

### 2. Используйте Tests для проверки

```javascript
// В Tests вкладке

// Проверка успешного ответа
pm.test("Status code is 200", function () {
  pm.response.to.have.status(200);
});

// Проверка наличия поля
pm.test("Response contains token", function () {
  pm.expect(pm.response.json()).to.have.property("token");
});

// Сохранение значения для следующих запросов
const responseJson = pm.response.json();
pm.environment.set("token", responseJson.token);
pm.environment.set("testId", responseJson.test._id);
```

### 3. Используйте переменные

```
{{baseUrl}} - базовый URL
{{token}} - JWT токен
{{userId}} - ID пользователя
{{groupId}} - ID группы
{{testId}} - ID теста
{{examId}} - ID экзамена
```

### 4. Экспорт результатов

**После выполнения тестов:**

1. Откройте **Collection Runner**
2. Нажмите **Run**
3. Выберите запросы для выполнения
4. Экспортируйте результаты

---

## 🛠️ Полезные Postman коллекции

### Переменные для разных окружений

**Development:**

```json
{
  "baseUrl": "http://localhost:5000",
  "token": "",
  "adminToken": ""
}
```

**Production:**

```json
{
  "baseUrl": "https://api.orbitest.com",
  "token": "",
  "adminToken": ""
}
```

---

## 📚 Использование документации в Postman

### 1. Просмотр документации

1. Откройте коллекцию
2. Нажмите на **eye icon** (Описание коллекции)
3. Вы увидите автоматически сгенерированную документацию

### 2. Добавление собственной документации

1. Откройте запрос
2. Нажмите на **Description** (внизу слева)
3. Добавьте описание и примеры

---

## 🔐 Безопасность в Postman

### ❌ НЕ делайте это:

```
❌ НЕ сохраняйте пароли в переменные
❌ НЕ коммитьте окружения с токенами в Git
❌ НЕ делитесь экспортами с токенами
```

### ✅ Делайте это:

```
✅ Используйте окружения без сохранения паролей
✅ Сохраняйте токены только в памяти (Initial value пусто)
✅ Используйте .gitignore для окружений
✅ Вращайте токены регулярно
```

---

## 📥 Импорт из других источников

### Из Swagger UI в Postman

1. Откройте Swagger UI: `http://localhost:5000/api-docs`
2. Нажмите на меню (три горизонтальные линии) справа вверху
3. Выберите "Download JSON"
4. Импортируйте в Postman обычным способом

### Из Postman в Swagger

1. Экспортируйте коллекцию из Postman
2. Конвертируйте в OpenAPI формат
3. Используйте свой swagger.json

---

## 🚀 Продвинутые техники

### 1. Цепочка запросов

```javascript
// В Tests вкладке первого запроса
const response = pm.response.json();
pm.environment.set("userId", response.user._id);

// Затем используйте {{userId}} в следующем запросе
```

### 2. Условные запросы

```javascript
// В Pre-request Script
if (pm.request.url.path.includes("admin")) {
  if (!pm.environment.get("adminToken")) {
    // Запросить admin token
  }
}
```

### 3. Автоматические тесты

Создайте коллекцию тестов для проверки:

- Валидности всех эндпоинтов
- Кодов ответов
- Схемы ответов
- Времени ответа

---

## 🐛 Решение проблем

### Ошибка: Cannot GET /swagger.json

- **Причина:** Сервер не запущен
- **Решение:** Запустите `npm start`

### Ошибка: 401 Unauthorized

- **Причина:** Токен неверный или истек
- **Решение:** Получите новый токен через POST /api/auth/login

### Ошибка: 403 Forbidden

- **Причина:** Недостаточно прав
- **Решение:** Используйте admin аккаунт для admin операций

### Токен не подставляется

- **Причина:** Неправильное имя переменной
- **Решение:** Используйте `{{token}}` (с двойными скобками)

---

## 📖 Дополнительные ресурсы

- [Postman Documentation](https://learning.postman.com)
- [OpenAPI Specification](https://spec.openapis.org)
- [REST API Best Practices](https://restfulapi.net)

---

**Готово к работе!** 🎉

Теперь вы можете:

- ✅ Импортировать API в Postman
- ✅ Организовать коллекции и окружения
- ✅ Автоматизировать тестирование
- ✅ Генерировать документацию

Начните с импорта коллекции и запустите первый запрос! 🚀
